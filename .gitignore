# ── 运行时数据：数据库、图片缓存、下载、导入产物 ──
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# ── 前端：依赖与构建产物（克隆后 cd static && npm install && npm run build）──
static/node_modules/
static/dist/

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
venv/
.venv/

# ── 环境与日志 ──
.env
*.log

# ── 打包交付物 ──
*.tar.gz

# ── 杂项 ──
.DS_Store
Thumbs.db
测试脚本
